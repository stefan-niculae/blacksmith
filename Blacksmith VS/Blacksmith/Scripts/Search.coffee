# CoffeeScript

$ ->
  console.log "aaa"
  initDataTables()
  
initDataTables = () ->
  #$("#mata").DataTable()
  #$(".results-grid").DataTable()
  # Convert dates from absolute time to distance from now